Installation
============

Use pip to install from PyPI::

    pip install django-routing

Add ``django_routing`` to your settings.py file::

    INSTALLED_APPS = (
        ...
        'django_routing',
        ...
    )
